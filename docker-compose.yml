version: "3"

services:
  db:
    image: postgres:13.1
    env_file:
      - environment.env
    restart: always
    healthcheck:
      test: "exit 0"
    volumes:
      - postgres:/var/lib/postgresql

  redis:
    image: redis:6.0
    restart: always

  backend: 
    build: .
    entrypoint: /app/entrypoint.sh
    ports:
      - "80:3000"
    volumes:
      - .:/app
    depends_on:
      - db
      - redis
    env_file:
      - environment.env
    restart: always
  
  sidekiq: 
    image: as3ar-elyoum-backend_backend
    command: 'bundle exec sidekiq -e $RAILS_ENV'
    volumes:
      - .:/app
    depends_on:
      - db
      - redis
      - backend
    env_file:
      - environment.env
    restart: always

volumes:
  postgres:
  bundle_path: