source_pages:
  cron: '* * * * *' # runs every minutes
  class: "SourcePagesCheckWorker"
  queue: default


products:
  cron: '* * * * *' # runs every minutes
  class: "ProductsCheckWorker"
  queue: default